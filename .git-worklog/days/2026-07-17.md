# Project Worklog — 2026-07-17

> 時區：Asia/Taipei
> Branch：main
> HEAD：1fa70cc

<!-- GIT_WORKLOG:2026-07-17:GENERATED:START -->
## 當日摘要
<!-- GIT_WORKLOG:SUMMARY:START -->
完成 v1.0 重構的 PR 4 語言契約並修補 evidence 驗證漏洞：日誌改以提問語言撰寫、引文改為對照 commit 當下的 tree 驗證。
<!-- GIT_WORKLOG:SUMMARY:END -->

參與者：Daniel Hsieh

## 主要異動

### 工作日誌目錄重構為 .git-worklog/

- **異動內容：** 將工作日誌從 PROJECT_WORKLOG/ 遷移至 .git-worklog/，並加入 days/ 子目錄、VERSION 檔案與 config.json 設定。
- **程式碼行為：** 專案工作日誌的位置改變。原本的 PROJECT_WORKLOG/<date>.md 移至 .git-worklog/days/<date>.md；PROJECT_WORKLOG/index.md 移至 .git-worklog/index.md。新增 VERSION（磁碟版面配置版本）與 config.json（schema_version、timezone、language、index_language 等設定）。標記前綴從 REPO_WORKLOG: 改為 GIT_WORKLOG:，但舊標記仍可讀取以支援遷移。自動偵測版面配置：優先讀取 days/，保持向後相容。
- **實作方式：** detect_layout() 函式探測版面配置而非假設；遷移指令自動選擇目錄優先。config.json 寫入首次寫入或遷移時，之後不再覆寫。markers.py 重新標記所有日期檔標記前綴。collect_git_history.py 排除舊 PROJECT_WORKLOG/ 目錄以防止自指 commit。
- **影響範圍：** 破壞性變更：現有 PROJECT_WORKLOG/ 目錄不再寫入；使用者需執行遷移指令轉換。所有後續腳本與 CLI 指向新目錄。測試新增 21 個（從 162 到 194）以涵蓋新遷移路徑與版面配置偵測。
- **相關檔案：** .git-worklog/VERSION、.git-worklog/config.json、.git-worklog/days/2026-07-15.md、.git-worklog/days/2026-07-16.md、.git-worklog/index.md、CHANGELOG.md、README.md、docs/naming-conventions.md、git-worklog/SKILL.md、git-worklog/agents/openai.yaml、git-worklog/references/code-analysis-rules.md、git-worklog/references/subagent-contract.md、git-worklog/scripts/check_worklog_coverage.py、git-worklog/scripts/collect_git_history.py、git-worklog/scripts/migrate_legacy_worklog.py、git-worklog/scripts/update_daily_worklog.py、git-worklog/scripts/validate_daily_worklog.py、git-worklog/scripts/validate_worklog_index.py、tests/helpers.py、tests/test_git_collection.py、tests/test_migrate.py、tests/test_pipeline.py、tests/test_report_scope.py
- **相關 commits：** 0539c91、397acf6、fab635d
- **測試與驗證：** tests/test_migrate.py — 舊單檔與目錄式遷移、標記重新標籤、位元完全相同驗證；tests/test_pipeline.py — 版面配置偵測、自指 commit 排除；tests/test_report_scope.py — 新目錄結構下的報告範圍、時區邊界
- **相容性與風險：** 遷移後 collect_git_history.py 必須同時排除舊 PROJECT_WORKLOG/ 和新 .git-worklog/ 目錄的自指 commit，否則會計算工作日誌編寫為真實工作；版面配置偵測依賴 days/ 目錄存在；interrupted 遷移 (部分檔案在 days/ 部分在根目錄) 會優先讀取 days/
- **維護注意事項：** 使用者需執行 `python3 scripts/migrate_legacy_worklog.py` 或 `/git-worklog migrate` 轉換舊目錄；config.json 為使用者檔案，確保資料目錄初始化後不覆寫它
- **後續事項：** language 與 index_language 欄位已在 config.json 但尚未實現（PR 4）

### git-worklog CLI 與 git_worklog 套件

- **異動內容：** 新增 `git-worklog` 命令列工具與 `git_worklog` Python 套件，包含 version、doctor、validate 三個初始指令。
- **程式碼行為：** 使用者可執行 `git-worklog version`（報告 CLI、磁碟版面配置、config schema 版本）、`git-worklog doctor`（環境能否執行工具檢查）、`git-worklog validate`（工作日誌磁碟格式驗證）。每個命令輸出單一 JSON 物件，`--text` 標旗呈現人類可讀格式。Exit 0 表正常、1 表執行成功但發現問題、2 表無法執行。
- **實作方式：** git_worklog 套件位於 git-worklog/git_worklog/（在 skill 目錄內，非並列），pyproject.toml 將套件根對應至該位置。複製 skill 資料夾仍可運作；pip install 也可將 git-worklog 安裝到 PATH。__version__ 是版本單一來源，pyproject.toml 動態讀取。markers.py 從 scripts/ 移至 git_worklog/ 共享定義；scripts/worklog_markers.py 變為轉接層。GIT_WORKLOG_HOME 預設為 ~/.git-worklog/，新增 GIT_WORKLOG_*_MODEL 變數；舊 REPO_WORKLOG_*_MODEL 仍支援但標記為已棄用。
- **影響範圍：** CLI 可獨立於 skill 運行，支援復本安裝模式不變。CI 新增測試：套件編譯、無安裝狀態下 CLI smoke 測試、pip install 後從中立工作目錄執行指令。測試從 194 增至 226。
- **相關檔案：** .github/workflows/ci.yml、.gitignore、CHANGELOG.md、README.md、docs/naming-conventions.md、git-worklog/git_worklog/__init__.py、git-worklog/git_worklog/__main__.py、git-worklog/git_worklog/cli/__init__.py、git-worklog/git_worklog/cli/doctor.py、git-worklog/git_worklog/cli/validate.py、git-worklog/git_worklog/cli/version.py、git-worklog/git_worklog/markers.py、git-worklog/git_worklog/paths.py、git-worklog/scripts/_bootstrap.py、git-worklog/scripts/worklog_markers.py、pyproject.toml、tests/test_cli.py、tests/test_provider_models.py
- **相關 commits：** 9df02e5、4fafab4
- **測試與驗證：** tests/test_cli.py — CLI 指令行解析、JSON 與 --text 輸出格式、exit codes；tests/test_provider_models.py — 模型設定解析
- **相容性與風險：** 複製 skill 資料夾安裝模式需確保 git_worklog 套件可被正確匯入
- **維護注意事項：** 舊 REPO_WORKLOG_*_MODEL 環境變數仍受尊重以維持向後相容
- **後續事項：** doctor 與 validate 的語言檢查暫時標記為 skipped（PR 4 實現）

### BCP 47 語言契約實現

- **異動內容：** 完整實現語言契約（roadmap §6.2）：BCP 47 語言標籤解析、全流程語言傳遞、日期檔與索引的語言決策、manifest 與結果的語言驗證、CLI 與文件的完整說明。
- **程式碼行為：** 工作日誌現用提問者的語言寫入，而非儲存庫的語言。優先序：使用者請求 → CLI 引數 → 專案設定 → agent host → 環境變數 → 系統 locale → 英文。BCP 47 標籤（zh-TW、en、ja）；裸 `zh` 拒絕、`zh-TW` 與 `zh-CN` 永不相同。日期檔宣告 `language` 且必須符合 manifest；索引首次建立時固定語言（config 釘選或首次建立時的戳記），避免多人團隊反覆改寫。摘要標記 `<!-- GIT_WORKLOG:SUMMARY:START -->` 讓索引語言無關地找到摘要。
- **實作方式：** language.py：resolve() 與 resolve_interface() 函式處理優先序解析；LanguageError 異常傳達失敗；normalize() 驗證 BCP 47 格式。config.py：load() 與 language()、index_language() 存取設定。markers.py：render_index() 依語言選擇索引欄位標題、render_summary() 將摘要括入標記。build_analysis_manifest.py：_resolve_language() 傳遞語言與來源。collect_day_results.py：_validate_language() 檢查結果語言符合 manifest。rebuild_worklog_index.py：索引語言決策與戳記。doctor.py 與 validate.py：語言設定檢查。
- **影響範圍：** 破壞性變更：day result 現需 `language` 欄位。manifest 新增 language 與 warnings。CLI 新增 --language 與 --language-source。doctor 與 validate 從 skipped 變為真實檢查。測試從 226 增至 301。索引與日期檔現支援多語言。
- **相關檔案：** CHANGELOG.md、README.md、docs/naming-conventions.md、git-worklog/SKILL.md、git-worklog/git_worklog/cli/doctor.py、git-worklog/git_worklog/cli/validate.py、git-worklog/git_worklog/config.py、git-worklog/git_worklog/language.py、git-worklog/git_worklog/markers.py、git-worklog/references/report-mode.md、git-worklog/references/subagent-contract.md、git-worklog/references/worklog-format.md、git-worklog/scripts/build_analysis_manifest.py、git-worklog/scripts/collect_day_results.py、git-worklog/scripts/rebuild_worklog_index.py、tests/test_cli.py、tests/test_day_results.py、tests/test_language.py、tests/test_worklog_engine.py
- **相關 commits：** 11589d5、a5b5be6、b98d16b、cd3bca6、61b5491、5dc5a73、402b0ab、74d2240、1a95bfc
- **測試與驗證：** tests/test_language.py — BCP 47 標籤正規化、優先序解析、locale 拒絕、簽名決策；tests/test_day_results.py — 結果語言驗證、manifest 不符檢測；tests/test_worklog_engine.py — 多語言日期檔、非 ASCII 完整往返、手寫區域跨語言保留、MANUAL 不被翻譯；tests/test_cli.py — --interface-language 旗標、不支援語言警告
- **相容性與風險：** agent host 語言決策位於 skill 外（SKILL.md §2a）；若 agent 未傳遞 --language，manifest 解析失敗；日期檔摘要標記前不存在的檔案在索引尋找摘要時退回到硬編碼 `當日摘要` 標題，不同語言日期會索引行空白
- **維護注意事項：** config.json language 欄位不回寫；已存在的設定永遠保留；索引語言來自 config 釘選、索引標記 lang= 屬性或首次建立時的 manifest 語言；英文以外的語言若無適配索引欄位標題，自動退回英文
- **後續事項：** 報告模式語言決策（report-mode.md）僅記錄於文件，未在指令實現

### 證據引文樹驗證

- **異動內容：** 修正 evidence 欄位檢查漏洞：現在驗證 `symbol` 與 `lines` 引文實際存在於引用的 commit，而非僅檢查 commit 與 file。
- **程式碼行為：** collect_day_results.py read 新增必要的 `--repo` 引數，檢查每個 evidence 條目：commit 存在、file 在該 commit 樹中存在、symbol 詞元出現在檔案中、lines 範圍在檔案內。引文不解析的日期失敗，阻止 apply。淺層複製的無法到達 commit 回報 EVIDENCE_UNVERIFIABLE 而非失敗（環境問題，非 subagent 責任）；完整複製中不存在的 hash 回報 EVIDENCE_COMMIT_UNKNOWN 並失敗。
- **實作方式：** _Tree 類別快取 git show 結果避免重複讀取；_verify_against_tree() 逐條目檢查；_SYMBOL_TOKEN_RE 正規表達式按詞元（3+ 字元）比對而非整字（因為 `class CacheLayer:` 與 `def get` 分散在原始碼）。
- **影響範圍：** 真實執行期間發現 7 個編造引文（migrate_directory 實為 parse_legacy、preview_dir 實為 previews_dir、行數超過檔案末尾、引用不存在的檔案）全部通過舊檢查。測試從 301 增至 315；舊 fixture 全為編造引文，改為建立真實雙 commit 儲存庫。
- **相關檔案：** CHANGELOG.md、git-worklog/SKILL.md、git-worklog/references/subagent-contract.md、git-worklog/scripts/collect_day_results.py
- **相關 commits：** 56e4a04、1fa70cc
- **測試與驗證：** tests/test_day_results.py — commit 存在檢查、file 在 commit 存在檢查、symbol 詞元出現檢查、lines 範圍檢查、淺層複製 EVIDENCE_UNVERIFIABLE
- **相容性與風險：** 淺層複製無法驗證遠端 commit；CI 預設淺層複製需明確指定 --unshallow
- **維護注意事項：** subagent 必須讀取實際代碼並驗證引文，不可編造符號或行數

## 修正項目

- {'title': 'evidence 引文驗證', 'summary': '修正 collect_day_results.py 未驗證 symbol 與 lines 引文的漏洞', 'files': ['git-worklog/scripts/collect_day_results.py'], 'commits': ['56e4a04'], 'evidence': [{'commit': '56e4a04', 'file': 'CHANGELOG.md', 'note': '真實執行期間發現 7 個編造引文，舊檢查全數通過'}]}

## 重構與技術債

- {'title': 'worklog_markers.py 模組遷移', 'summary': 'markers.py 從 scripts/ 移至 git_worklog/ 供 CLI 與腳本共享；scripts/worklog_markers.py 變轉接層', 'files': ['git-worklog/scripts/worklog_markers.py', 'git-worklog/git_worklog/markers.py'], 'commits': ['9df02e5'], 'evidence': [{'commit': '9df02e5', 'file': 'README.md', 'note': 'scripts/worklog_markers.py 現為相容轉接層'}]}

## 測試狀態

- tests/test_cli.py — CLI 指令、JSON 輸出、--text 格式、--interface-language
- tests/test_day_results.py — 語言驗證、引文樹檢查
- tests/test_language.py — BCP 47 解析、優先序、環境變數、簽名
- tests/test_migrate.py — 遷移位元完全相同、標記重新標籤
- tests/test_pipeline.py — 完整管道、版面配置偵測
- tests/test_worklog_engine.py — 多語言、非 ASCII、MANUAL 保留、索引簽名

## 接手者快速閱讀

- PR #11（目錄重構）、#13（CLI foundation）、#14（語言契約）、#16（引文驗證）已合併至 main。下一步：以實際執行驗證語言契約端到端功能、考慮 subagent 是否需額外詞彙或指導。
- 報告模式語言決策（report-mode.md §5）已記錄但未在程式碼實現，待後續 PR 引出。
- 淺層複製環境（如 CI）的引文驗證需特別照顧；--unshallow 可完整檢查。
<!-- GIT_WORKLOG:2026-07-17:GENERATED:END -->

<!-- GIT_WORKLOG:2026-07-17:MANUAL:START -->

<!-- GIT_WORKLOG:2026-07-17:MANUAL:END -->
