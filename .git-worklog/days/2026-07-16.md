# Project Worklog — 2026-07-16

> 時區：Asia/Taipei
> Branch：main
> HEAD：da426e7

<!-- GIT_WORKLOG:2026-07-16:GENERATED:START -->
## 當日摘要

連續發佈三個版本並開出 v0.4.0 兩大功能：目錄型日誌（v0.2.0）、成本優先模型單一設定源（v0.3.0）、自指 commit 修復（v0.3.1），再加上 commit 作者入帳與唯讀報告模式。

v0.2.0 把單檔日誌重構成一天一檔加索引，寫入改為交易式；v0.3.0 讓模型選擇收斂到 config/provider_models.json 單一來源，預設改為成本優先且不再靜默降級；v0.3.1 修掉「日誌記錄自己在寫日誌」的自指 commit。之後 v0.4.0 讓 commit 作者進入 manifest 與日誌，並新增唯讀的報告模式（週報／CHANGELOG／交接），測試從 92 成長到 152。

參與者：Daniel Hsieh

## 主要異動

### v0.2.0 — 目錄型工作日誌結構（重大改變）

- **異動內容：** 將單檔案工作日誌（docs/PROJECT_WORKLOG.md）重構為目錄結構（PROJECT_WORKLOG/<date>.md + index.md）。每日單獨一個檔案，重新分析只改寫當日檔案；索引從日檔自動重建，保留 MANUAL 區塊。支援自動轉移舊單檔格式。
- **程式碼行為：** 工作日誌儲存位置從 docs/PROJECT_WORKLOG.md 改為 PROJECT_WORKLOG/ 目錄，日誌內容按日期分散在多個 .md 檔；索引 index.md 自動從日檔重建，日誌編輯區 (MANUAL) 在單檔與日檔版本間自動保留。
- **實作方式：** 新增 worklog_markers.py 重寫（日檔與索引格式、標記符號解析）；update_daily_worklog.py 改為事務型多檔寫入器（支援復原）；新增 rebuild_worklog_index.py（從日檔重建索引）；新增兩支驗證腳本（validate_daily_worklog.py / validate_worklog_index.py）；新增 migrate_legacy_worklog.py 一次性轉移舊格式；新增 preview_state.py 多檔指紋。移除舊的 update_worklog.py / validate_worklog.py。
- **影響範圍：** Breaking change：現存單檔工作日誌需執行 /repo_worklog migrate 轉移。結構改變後支援更精細的日誌管理、可併行編輯、更清晰的版本控制。
- **相關檔案：**
  - `repo_worklog/scripts/worklog_markers.py`
  - `repo_worklog/scripts/update_daily_worklog.py`
  - `repo_worklog/scripts/rebuild_worklog_index.py`
  - `repo_worklog/scripts/validate_daily_worklog.py`
  - `repo_worklog/scripts/validate_worklog_index.py`
  - `repo_worklog/scripts/migrate_legacy_worklog.py`
  - `repo_worklog/scripts/preview_state.py`
- **相關 commits：**
  - 45f57b3 (Daniel Hsieh) feat(worklog)!: store the worklog as a per-day PROJECT_WORKLOG/ directory
  - b1e7101 (Daniel Hsieh) docs: document the directory-based worklog layout
  - 23a1801 (Daniel Hsieh) chore(release): v0.2.0
  - eaf46f4 (Daniel Hsieh) docs: add CHANGELOG with 0.1.0 and 0.2.0 history
- **測試與驗證：** tests/test_worklog_writers.py — 65 個測試涵蓋日檔與索引格式、MANUAL 保留、事務型復原、標記驗證
- **相容性與風險：**
  - Breaking change 需執行轉移；轉移腳本設計保守（不刪舊檔、已存在檔案不覆寫）
  - 事務型寫入複雜度高，多個失敗點需完善測試
- **維護注意事項：**
  - MANUAL 分隔符號硬編在 worklog_markers.py，改格式需同步更新
  - rebuild_worklog_index.py 依賴 YYYY-MM-DD.md 命名規範
- **後續事項：**
  - 驗證轉移邏輯完整性（45f57b3 後續補檔時需確認無孤立標記）
  - 無自動修復機制，使用者編輯日誌檔需手動調整或重跑

### v0.3.0 — 單一源配置的成本優先模型選擇

- **異動內容：** 實現跨主機（Anthropic/OpenAI/Google）模型選擇的單一源：新增 config/provider_models.json 為機器可讀源，成本優先預設（Claude Haiku 4.5 / GPT-5.6 Luna @ low / Gemini 3.5 Flash）；resolve_provider_model.py 以覆蓋優先級（CLI > env > config）解析，失敗時明確列出候選。
- **程式碼行為：** Day Subagent 預設模型從 Sonnet 5 / GPT-5.6 Terra / Gemini 3 Flash 改為 Haiku 4.5 / GPT-5.6 Luna / Gemini 3.5 Flash；manifest 攜帶結構化 model 物件；build_analysis_manifest.py 改 --model-json；manifest commits[] 加 author_name。
- **實作方式：** 新增 config/provider_models.json、scripts/resolve_provider_model.py；改造 build_analysis_manifest.py (_parse_model() 驗證)；agents/openai.yaml 移除 providers 區塊；manifest 投影加 author_name。
- **影響範圍：** 模型行為集中在 config/provider_models.json；escalation 與預設解耦；失敗時給清楚錯誤與候選，不默認降級。
- **相關檔案：**
  - `repo_worklog/config/provider_models.json`
  - `repo_worklog/scripts/resolve_provider_model.py`
  - `repo_worklog/scripts/build_analysis_manifest.py`
  - `repo_worklog/agents/openai.yaml`
  - `repo_worklog/references/provider-models.md`
- **相關 commits：**
  - 5ceda82 (Daniel Hsieh) feat(models)!: cost-first per-host subagent models via single-source config
  - 94b8e82 (Daniel Hsieh) test(models): cover provider resolution, manifest, fallback, escalation
  - 4ac7842 (Daniel Hsieh) docs(models): document per-host model config, overrides, and escalation
  - 11ec5a0 (Daniel Hsieh) docs: record 0.3.0 in CHANGELOG
  - c4c2957 (Daniel Hsieh) chore(release): v0.3.0
- **測試與驗證：** tests/test_provider_models.py — env/CLI 覆蓋、未知主機拒絕、空 model id 拒絕、escalation 檢查
- **相容性與風險：**
  - Breaking change：舊 --model 字串失效；env 變數需重新配置
  - escalation 無配置時改為拒絕 (NO_ESCALATION_MODEL)，可能破壞退化行為
- **維護注意事項：**
  - config/provider_models.json 是決定性源；任何模型變更先改這裡
  - reasoning_effort 只出現在 openai；absent 時欄位完全不含
- **後續事項：**
  - Monitor escalation_policy 的自動化開關與成本控制
  - 確認 escalation_model_id 與 reasoning_effort 組合規則完整

### v0.3.1 — 排除自指工作日誌 commit（修復）

- **異動內容：** 修復 collect_git_history.py 不必要地包含只改動工作日誌目錄的 commit（如 'chore(docs): 補充日誌'），造成自指日誌。現完全排除此類 commit，無此類 commit 的日期回報 has_changes:false。
- **程式碼行為：** 只修改 PROJECT_WORKLOG/ 的 commit 完全排除；也修改其他檔案的 commit 則保留，但 files[] 中移除工作日誌目錄。此類日期 commit_count:0、has_changes:false，無空洞「今天我寫日誌」項。
- **實作方式：** collect_git_history.py 新增 _in_worklog_dir() 與 DEFAULT_WORKLOG_DIR；collect_commits() 加 worklog_dir 參數；篩選：non-worklog files 為空則排除，否則保留。
- **影響範圍：** 日誌品質提升：無自指條目；有真正工作時才有工作項。一致性：與 worklog-format.md §6 對稱。
- **相關檔案：**
  - `repo_worklog/scripts/collect_git_history.py`
  - `tests/test_git_collection.py`
- **相關 commits：**
  - d849ba7 (Daniel Hsieh) fix(collect): exclude self-referential worklog commits
  - 79e649d (Daniel Hsieh) test(collect): cover self-referential worklog commit exclusion
  - 93662fe (Daniel Hsieh) docs: document self-referential worklog commit exclusion
  - 85f6fca (Daniel Hsieh) docs: record 0.3.1 in CHANGELOG
  - e9db5ba (Daniel Hsieh) chore(release): v0.3.1
- **測試與驗證：** tests/test_git_collection.py — 只改日誌 commit 被排除、混合 commit 日誌檔保留
- **相容性與風險：**
  - 影響既有日誌補齊；重跑時 commit 計數會改變（現為 0）
  - 自指定義（只改 --worklog-dir）邊界不清：symlink、子模組、跨界檔案是否正確處理
- **維護注意事項：**
  - DEFAULT_WORKLOG_DIR 與 worklog_markers.py WORKLOG_DIRNAME 保持同步
  - --worklog-dir 參數需 orchestrator 正確傳遞
- **後續事項：** 驗證混合型 commit 的 files[] 過濾完整性

### v0.4.0 Part 1 — Commit 作者進入日誌與 manifest

- **異動內容：** Commit 作者（author_name）現保留進 manifest。build_analysis_manifest.py 新增 authors[] 列表（去重、首現順序排列），commits[] 各項包含 author_name。Orchestrator 從 manifest 直接渲染「參與者」與作者，Day Subagent 無需決定。
- **程式碼行為：** manifest 新增 authors[] 與 commits[].author_name；日誌 「當日摘要」 下加 「參與者」 行；相關 commits 格式改為 「<short_hash> (<author_name>) <subject>」。
- **實作方式：** build_analysis_manifest.py 新增 _collect_authors()、commits[] 投影加 author_name；worklog-format.md §3 模板更新；subagent-contract.md 說明；tests/helpers.py 加 _commit(..., author=...) 與 make_multi_author_repo() fixture。
- **影響範圍：** 多人專案日誌完整記錄「誰做的」；作者歸屬是確定性事實（腳本處理），避免模型漏人；Subagent 可用作分析但無權決定渲染。
- **相關檔案：**
  - `repo_worklog/scripts/build_analysis_manifest.py`
  - `repo_worklog/references/worklog-format.md`
  - `repo_worklog/references/subagent-contract.md`
  - `repo_worklog/SKILL.md`
  - `tests/helpers.py`
- **相關 commits：**
  - 4d08ee4 (Daniel Hsieh) feat(manifest): carry commit author into the analysis manifest
  - b92c4ea (Daniel Hsieh) test(manifest): cover commit author in the analysis manifest
  - fea6f5a (Daniel Hsieh) docs(format): record authorship in the daily worklog
- **測試與驗證：** tests/test_git_collection.py::TestManifestAuthors — 去重、首現順序、無 commit 日無作者
- **相容性與風險：**
  - 多言語名字（中文、特殊符號）UTF-8 編碼正確性
  - Email 故意排除（PII），若需用 email 辨別同名人物無法回溯
  - Merge commit 無 files[] 但有 author_name 的 edge case
- **維護注意事項：**
  - _collect_authors() 的 order 依首現；若需字母序需改此函數
  - worklog-format.md §3 約束 orchestrator 不重新推導作者
- **後續事項：** 驗證 orchestrator 實現正確查表 short_hash → author_name

### v0.4.0 Part 2 — 唯讀報告模式與 scope 解析

- **異動內容：** 新增報告模式：讀既有日誌回答問題（週報、CHANGELOG、交接、個人貢獻），純讀取無寫入。支援 date scope（按日期）與 ref scope（按 git tag）。新增 resolve_ref_range.py（tag → commit 集合 → 日期）、check_worklog_coverage.py（日期 → 缺口偵測）、resolve_date_range.py --max-days（報告讀 90 日 vs 生成 30 日）。
- **程式碼行為：** 報告模式詢問與生成模式分離（SKILL.md §1 新增路由）；報告輸出僅到對話（無寫檔、無 dry-run）；遇缺口先詢問補齊或直接報告；ref scope 以 commit 集合為權威、日期作索引；date 日曆週正規化（「上一週」vs「最近一週」區分）。
- **實作方式：** 新建 scripts/resolve_ref_range.py（tag → commits + dates）；新建 scripts/check_worklog_coverage.py（日期 → covered/gap/no-commits）；resolve_date_range.py 加 --max-days；SKILL.md 加模式路由；新增 references/report-mode.md；date-parameter-contract.md 加區間定義；docs/plans/ 新增計畫（yyyy-MM-dd 命名）；README 加範例；新增 tests/test_report_scope.py。
- **影響範圍：** 日誌現可被查詢彙整（週報、CHANGELOG、交接）；報告與生成各自成本邊界清晰；gap 主動偵測與提示，不默默降低品質。
- **相關檔案：**
  - `repo_worklog/scripts/resolve_ref_range.py`
  - `repo_worklog/scripts/check_worklog_coverage.py`
  - `repo_worklog/scripts/resolve_date_range.py`
  - `repo_worklog/SKILL.md`
  - `repo_worklog/references/report-mode.md`
  - `repo_worklog/references/date-parameter-contract.md`
  - `docs/plans/2026-07-16-commit-author-and-report-mode.md`
  - `README.md`
  - `tests/test_report_scope.py`
- **相關 commits：**
  - 01393e4 (Daniel Hsieh) feat(report): add scope resolution for read-only report mode
  - 286ddd8 (Daniel Hsieh) test(report): cover report-mode scope resolution
  - 889a270 (Daniel Hsieh) docs(skill): add read-only report mode
  - 5b3cc3c (Daniel Hsieh) docs(plans): collect design plans under docs/plans with dated filenames
  - 70c7189 (Daniel Hsieh) chore(release): v0.4.0
- **測試與驗證：**
  - tests/test_report_scope.py::TestResolveRefRange — tag 解析、首個 tag、無 tag 拒絕、annotated vs lightweight
  - tests/test_report_scope.py::TestCheckWorklogCoverage — 三種 status 分類、gap vs no-commits 區分
  - tests/test_report_scope.py::TestResolveDateRange — --max-days 預設/覆寫/超限
  - tests/helpers.py::make_tagged_repo — 多 tag 與跨日 fixture
- **相容性與風險：**
  - ref scope 權威性檢查（只描述指定 range commit）靠模型遵守，無硬性機制；模型漏掉會混入範圍外 commit
  - 90 日讀限是拍板數字，context 實際撐不住可能需下修
  - Gap 詢問流程（補齊/跳過/取消）在 SKILL.md 文本說明，orchestrator 實現需完全遵守
  - 自指排除邏輯透過 check_worklog_coverage.py 重複調用 collect_commits()，若邏輯飄散會產生偽差
- **維護注意事項：**
  - resolve_ref_range.py 使用 git log creatordate 排序 tag，需 Git 2.7+
  - check_worklog_coverage.py 重用 collect_commits()，須傳遞相同 --worklog-dir
  - 「上一週」vs「最近一週」區別需 orchestrator NL 正規化層實現
  - 90 日讀取 disk I/O 量需監控性能
- **後續事項：**
  - 手動驗證本 repo 的 v0.3.1 tag 範圍（已有 v0.1.0~v0.3.1 四個 tag）
  - Gap 詢問流程 UX 測試：使用者是否理解補齊 vs 直接報告差別
  - 報告合成邏輯（讀檔、摘取、聚合）在 orchestrator 層；驗證不會遺漏此責任
  - 90 日讀取是否需 cache 或批量操作

## 修正項目

- **fix(collect): 排除自指工作日誌 commit**：d849ba7：collect_git_history.py 排除只改工作日誌目錄的 commit。

## 設定、CI 與部署

- 
- 
- ****：v0.2.0 → v0.3.1 → v0.4.0 累進管線

## 測試狀態

- Suite: 97 → 103 → 132 across all commits
- Fixtures: make_multi_author_repo(), make_tagged_repo()
- Coverage: author attribution, ref-range resolution, worklog coverage detection, provider model resolution, date range validation

## 接手者快速閱讀

- 建議優先閱讀：`repo_worklog/references/report-mode.md`（報告模式規格）、`repo_worklog/references/subagent-contract.md` §6a（結果以檔案交換）
- 關鍵流程入口：報告模式由 `SKILL.md` §1a 路由；ref scope 走 `resolve_ref_range.py`，覆蓋檢查走 `check_worklog_coverage.py`
- 容易踩雷：版本範圍以 **commit 集合**為權威而非日期區間；「沒有 commit 的日期」不算缺口，不可拿去補齊
- 容易踩雷：30 天上限只管產生與補齊（會派 subagent）；報告模式讀取上限為 90 天
- 未完成：ref scope 的「只描述範圍內 commit」目前靠模型遵守，無強制機制；90 天上限未經實測

## 待確認事項

- **ref scope 權威性檢查**：報告模式應只描述 tag 範圍的 commit，即使日誌含其他 commit。目前靠 orchestrator 模型遵守指示，無硬性機制。
- **90 日讀限可持續性**：報告模式讀取上限 90 天是拍板決策，未經 context 實測。實際讀取可能撐不住。
- **Gap 偵測精度**：no-commits vs gap 分類依 collect_commits() 排除自指 commit。若邏輯在兩處飄散會產生偽差。
<!-- GIT_WORKLOG:2026-07-16:GENERATED:END -->

<!-- GIT_WORKLOG:2026-07-16:MANUAL:START -->

<!-- GIT_WORKLOG:2026-07-16:MANUAL:END -->
